import React from 'react'

const footerStyle = {
  textAlign: "center",
  color: "white"
}

const Footer = props => {
  return (
    <footer style={footerStyle}>
      <p>&copy; GaigoNikki</p>
    </footer>
  )
}

export default Footer